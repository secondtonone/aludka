export interface ContractProps {
  id: string;
  prizePercentage: string;
  round: number;
  commissionPercentage: string;
  entryFee: string;
  totalPlayers: number;
  totalAmount: string;
  balance: string;
}
