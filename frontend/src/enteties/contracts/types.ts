export interface ContractProps {
  id: string;
  prizePercentage: number;
  round: number;
  commissionPercentage: number;
  entryFee: number;
  totalPlayers: number;
  totalAmount: string;
  balance: string;
}
